version: "3"

services:
  meta:
    build: .
    command: cron
    volumes:
      - "./caches:/app/caches"
      - "./upstream:/app/upstream"
      - "./prismlauncher:/app/prismlauncher"
      - "./public:/app/public"
      - "./config:/app/config"
    restart: unless-stopped
